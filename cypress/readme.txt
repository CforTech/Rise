How to Set Up a Cypress Project
Prerequisites:

Ensure Node.js (version 12 or higher) is installed. Download Node.js.

Install NPM (comes with Node.js) or Yarn.
Download IDE e.g. visual studio code
1.Open the project folder from visual studio code:

2. Install Cypress from the terminal or CMD


3. Open Cypress:

4. Run Tests: npx cypress open.
